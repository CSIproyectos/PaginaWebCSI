---
import Layout from '@/layouts/Layout.astro';
---

<Layout
  title="Contacto - Proyectos CSI"
  description="Página en desarrollo."
>
  <section class="min-h-screen flex items-center justify-center bg-gray-50">
    <div class="container-custom text-center">
      <div class="max-w-2xl mx-auto">
        <h1 class="mb-6 text-primary-900">Página en Desarrollo</h1>
        <p class="text-xl text-gray-600 mb-8">
          Estamos trabajando en esta sección. Pronto estará disponible.
        </p>
        <a href="/" class="btn-primary">
          Volver al Inicio
        </a>
      </div>
    </div>
  </section>
</Layout>

      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12">
        <!-- Contact Form -->
        <div class="bg-white rounded-xl shadow-lg p-8">
          <form id="contact-form" class="space-y-6">
            <div>
              <label for="name" class="block text-sm font-semibold text-gray-700 mb-2">
                Nombre Completo *
              </label>
              <input
                type="text"
                id="name"
                name="name"
                required
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-transparent"
                placeholder="Juan Pérez"
              />
            </div>

            <div>
              <label for="email" class="block text-sm font-semibold text-gray-700 mb-2">
                Correo Electrónico *
              </label>
              <input
                type="email"
                id="email"
                name="email"
                required
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-transparent"
                placeholder="juan@empresa.com"
              />
            </div>

            <div>
              <label for="phone" class="block text-sm font-semibold text-gray-700 mb-2">
                Teléfono *
              </label>
              <input
                type="tel"
                id="phone"
                name="phone"
                required
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-transparent"
                placeholder="+52 55 1234 5678"
              />
            </div>

            <div>
              <label for="company" class="block text-sm font-semibold text-gray-700 mb-2">
                Empresa
              </label>
              <input
                type="text"
                id="company"
                name="company"
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-transparent"
                placeholder="Nombre de tu empresa"
              />
            </div>

            <div>
              <label for="service" class="block text-sm font-semibold text-gray-700 mb-2">
                Servicio de Interés *
              </label>
              <select
                id="service"
                name="service"
                required
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-transparent"
              >
                <option value="">Selecciona un servicio</option>
                <option value="videovigilancia">Videovigilancia</option>
                <option value="control-acceso">Control de Acceso</option>
                <option value="automatizacion">Automatización</option>
                <option value="cableado">Cableado Estructurado</option>
                <option value="consultoria">Consultoría</option>
                <option value="otro">Otro</option>
              </select>
            </div>

            <div>
              <label for="message" class="block text-sm font-semibold text-gray-700 mb-2">
                Mensaje *
              </label>
              <textarea
                id="message"
                name="message"
                required
                rows="5"
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-transparent"
                placeholder="Cuéntanos sobre tu proyecto..."
              ></textarea>
            </div>

            <div>
              <button type="submit" class="btn-primary w-full">
                Enviar Mensaje
              </button>
            </div>

            <p class="text-sm text-gray-500 text-center">
              Al enviar este formulario aceptas nuestro <a href="/aviso-privacidad" class="text-primary-600 hover:underline">Aviso de Privacidad</a>
            </p>
          </form>

          <div id="form-success" class="hidden mt-6 p-4 bg-green-50 border border-green-200 rounded-lg text-green-800">
            ¡Gracias por contactarnos! Nos pondremos en contacto contigo pronto.
          </div>

          <div id="form-error" class="hidden mt-6 p-4 bg-red-50 border border-red-200 rounded-lg text-red-800">
            Hubo un error al enviar el formulario. Por favor intenta de nuevo o contáctanos directamente.
          </div>
        </div>

        <!-- Contact Info -->
        <div class="space-y-8">
          <div>
            <h2 class="text-2xl font-bold mb-6">Información de Contacto</h2>
            
            <div class="space-y-6">
              <div class="flex items-start">
                <div class="flex-shrink-0 w-12 h-12 bg-primary-100 rounded-lg flex items-center justify-center">
                  <svg class="w-6 h-6 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                  </svg>
                </div>
                <div class="ml-4">
                  <h3 class="font-semibold text-lg mb-1">Email</h3>
                  <a href="mailto:contacto@proyectoscsi.mx" class="text-gray-600 hover:text-primary-600">
                    contacto@proyectoscsi.mx
                  </a>
                </div>
              </div>

              <div class="flex items-start">
                <div class="flex-shrink-0 w-12 h-12 bg-primary-100 rounded-lg flex items-center justify-center">
                  <svg class="w-6 h-6 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
                  </svg>
                </div>
                <div class="ml-4">
                  <h3 class="font-semibold text-lg mb-1">Teléfono</h3>
                  <p class="text-gray-600">+52 (55) XXXX-XXXX</p>
                </div>
              </div>

              <div class="flex items-start">
                <div class="flex-shrink-0 w-12 h-12 bg-primary-100 rounded-lg flex items-center justify-center">
                  <svg class="w-6 h-6 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
                  </svg>
                </div>
                <div class="ml-4">
                  <h3 class="font-semibold text-lg mb-1">Ubicación</h3>
                  <p class="text-gray-600">Ciudad de México, México</p>
                </div>
              </div>

              <div class="flex items-start">
                <div class="flex-shrink-0 w-12 h-12 bg-primary-100 rounded-lg flex items-center justify-center">
                  <svg class="w-6 h-6 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                  </svg>
                </div>
                <div class="ml-4">
                  <h3 class="font-semibold text-lg mb-1">Horario</h3>
                  <p class="text-gray-600">Lunes - Viernes: 9:00 AM - 6:00 PM</p>
                  <p class="text-gray-600">Sábado: 10:00 AM - 2:00 PM</p>
                </div>
              </div>
            </div>
          </div>

          <!-- Map placeholder -->
          <div class="bg-gray-200 rounded-xl h-64 flex items-center justify-center">
            <p class="text-gray-500">Mapa de ubicación</p>
            <!-- Agregar iframe de Google Maps aquí -->
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>

<script>
  // Simple form handling - Replace with HubSpot Forms API integration
  const form = document.getElementById('contact-form') as HTMLFormElement;
  const successMessage = document.getElementById('form-success');
  const errorMessage = document.getElementById('form-error');

  if (form) {
    form.addEventListener('submit', async (e) => {
      e.preventDefault();

      const formData = new FormData(form);
      const data = Object.fromEntries(formData);

      try {
        // TODO: Integrate with HubSpot Forms API
        // Example endpoint: https://api.hsforms.com/submissions/v3/integration/submit/:portalId/:formGuid
        
        // For now, just show success message
        console.log('Form data:', data);
        
        // Simulate API call
        await new Promise(resolve => setTimeout(resolve, 1000));

        form.classList.add('hidden');
        successMessage?.classList.remove('hidden');

        // Optional: Send to your backend or email service
      } catch (error) {
        console.error('Form submission error:', error);
        errorMessage?.classList.remove('hidden');
      }
    });
  }
</script>
